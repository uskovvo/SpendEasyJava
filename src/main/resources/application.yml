spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/app_db
    username: admin
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: sELxKYVjNSgXVdVehhzZtvTY5DDROovM8L8y5W9jfL8=
      expiration: 86400000
      refresh-token:
        expiration: 604800000
